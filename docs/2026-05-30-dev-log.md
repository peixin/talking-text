# 2026-05-30 开发日志 — 拆分「采集」与「成书」，教材这块解套

> 本次解决长期困扰的"教材这块很混乱"问题。根因诊断、概念模型与三层设计写在
> [`content-lifecycle.md`](content-lifecycle.md)（中文版 [`content-lifecycle.cn.md`](content-lifecycle.cn.md)）。
> 本日志记录据此对代码做的重构（代号 B），以及随手清理的存量 mypy 债务。
>
> 不改数据库 schema。后端 API 行为有一处契约变更（`levels` → `tag_path`）。

---

## 0. 起点 — 为什么"混乱"

教材系统把两件本该分开的事压成了一件：**采集碎片**（每天拍照/输入）与
**拼装成书**（Book→Unit→Lesson 树）。旧实现让每张照片在采集那一刻就"自动归位"
进树——AI 推断 `parent_id`、把整库塞进 prompt、按位置猜 `kind`、`levels` 数组模糊
并树。归档这件事本质模糊、易错、又跟核心循环无关，却被做成了自动主路径，于是三个
症状同时出现：层级太复杂、UI 乱、概念模型没想清。

确立的原则（详见 content-lifecycle.md §1）：

> **采集是 append-only 且"笨"的；结构化是另一个、刻意的动作，由极少数人（先是运营者）
> 完成，绝不强加到每张照片上。**

并区分了两类用户（跟随者 / 贡献者）与"渐进式"的两个含义（消费渐进 / 创作渐进）。

---

## 1. 本次代码改动（B，6 个切片）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `backend/app/api/ingest.py` | 抽取不再当图书管理员：prompt 删掉 `parent_id`/`levels`/book/unit/lesson/kind_label 推断，metadata 只回 `suggested_name`+`cefr_level`+`confidence`；删掉"把整库塞进 prompt"那段（连带 `db`/`select`/`ItemGroup` 依赖）；`_parse_extraction` 的 levels 兜底移除。 |
| 2 | `backend/app/api/groups.py` | `levels` → `tag_path`；create/update 两套并树逻辑合并成单个 `_assemble_tag_path` helper；**删掉按位置猜 kind**，新建节点统一 `kind="tag"`（无类型标签）；顺手修复**多层路径不给 learner 分配**的潜在 bug（现在分配的是路径的 ROOT，且幂等）。 |
| 3 | `frontend/lib/backend.ts`·`lib/api.ts`·`parent/materials/actions.ts` | 类型 `levels`→`tag_path`；`ExtractedMetadata` 砍到 `suggested_name`+`cefr_level`+`confidence`。 |
| 4 | `chat/[sessionId]/IngestDrawerClient.tsx` | 采集变扁平词袋（一个可编辑名字 + 词表）；删客户端模糊建树（`getAutocompleteOptions`/`getMatchedGroupNode`）与层级卡片 UI；移除不再使用的 `groups` prop（同步改 `ChatClient.tsx`）。 |
| 5 | `parent/materials/MaterialsClient.tsx` | 同 #4，第二份采集流程也扁平化。 |
| 6 | `parent/materials/[groupId]/GroupDetailClient.tsx` | 改用 `tag_path` API；UI 去掉 "Level N" 措辞，改为"标签路径（由根部到叶子）"。此处的整理建树是合法的（整理时、人确认），故保留其匹配联想逻辑——它是整理工作台的雏形。 |

新增 i18n key：`Ingest.capture_name_label` / `capture_name_placeholder`（en/zh-CN/zh-TW）。

### 契约变更说明
- `POST /groups` 与 `PATCH /groups/{id}` 的请求体字段 `levels` 更名为 `tag_path`，
  语义不变（有序精确标签名，root→leaf，确定性嵌套+同名合并）。前端已同步。
- 抽取接口 `POST /ingest/extract` 的返回 `metadata` 结构收窄；前端原本就用 `|| 兜底`
  读取，已改为读 `suggested_name`。

---

## 2. 随手清理的存量 mypy 债务

以下错误来自更早提交的代码，并非 B 引入，但在 B 改动的文件里，顺手修掉以便
`just typecheck` 更干净：

- `groups.py` `_verify_write_permission`：`curr` 标注为 `ItemGroup | None`。
- `groups.py` `unassign_learner`：`result.rowcount` 用 `cast("CursorResult[Any]", …)`。
- `storage/models/content.py`：`parent_to_children` 加 `dict[uuid.UUID, list[uuid.UUID]]` 标注。
- `core/dialog/orchestrator.py`：`_spawn_background` 的 `task` 加 `asyncio.Task[None]` 标注。

mypy 错误从 19 → 8。**剩余 8 个仍是存量、且与本次无关、改动有风险，本次不碰：**
- `adapters/llm/volc.py`（5 个）：OpenAI `ChatCompletion*MessageParam` TypedDict 类型。
- `api/conversation.py`（3 个）：`b64encode(bytes | None)`、`audio_format` 的 `Literal | None → str`。
  其中 `b64encode` 处可能是真实隐患，建议后续单独排查（需运行时验证，故未在本次自动改）。

---

## 3. 验证状态

- 后端：`ruff check` ✓、`ruff format` ✓；`mypy` 在本次所有改动文件上 ✓（剩余 8 个为上述存量）。
- 前端：`tsc --noEmit` ✓、`eslint` ✓、`prettier` ✓。
- **未做运行时验证**（按协作规则，启动服务与端到端验证由用户发起）。

需要用户手测的关键路径：
1. chat 内拍照/输入 → 采集为扁平词袋（名字来自 `suggested_name`，可改）。
2. parent/materials 内同样的采集。
3. `GroupDetail` 保存（走 `tag_path`，确定性建树/同名合并）。
4. **注意**：「采集即自动建树」已按设计移除；建树现在只发生在 GroupDetail / 未来的整理工作台。

---

## 4. 遗留与后续

- `GroupDetailClient` 内部状态变量仍叫 `levels`（仅内部名，API 已发 `tag_path`）。
  它将在"完整 inbox + 拖拽整理工作台"（content-lifecycle.md §4.2 / §4.3）一步整体重写。
- 整理工作台的第二股来源——**练习派生候选**（从 `turn` 文本 diff 出"说过但未收进教材的词"，
  content-lifecycle.md §4.3）——尚未实现，是把核心循环闭合成"边界外推"的关键后续。
- `get_descendant_group_ids` 仍是全表扫描，后续可换递归 CTE（与本次无关）。
- 冷启动：库需要运营者先用整理工作台塞进 1–2 本干净 canonical 教材，跟随者路径才成立。
